# TradeCommand — personal investing command-center dashboard.
# Stdlib-only backend that reads the trading bot's state files, polls market
# data, talks to the Robinhood MCP (direct OAuth client, claude -p fallback),
# and serves the PWA in dashboard/static/.
